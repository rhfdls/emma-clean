namespace Emma.Models.Enums;

/// <summary>
/// Represents the status of a contact assignment (service provider assignment).
/// </summary>
public enum ContactAssignmentStatus
{
    Active = 0,
    Completed = 1,
    Cancelled = 2,
    OnHold = 3,
    Expired = 4
}
