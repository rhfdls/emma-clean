# Emma AI Platform - Environment Configuration Example
#
# 1. Copy this file to .env for default values (non-sensitive)
# 2. Copy again to .env.local for your actual secrets (never commit .env or .env.local)
# 3. Both .env and .env.local are gitignored for safety
# 4. Docker Compose and Windsurf local builds will load these automatically
#
# NEVER commit real secrets to the repo

# =========================================================================
# CONFIG PRECEDENCE:
# 1. Docker Compose 'environment:' entries (highest precedence)
# 2. .env.local file values
# 3. .env file values
# 4. appsettings.*.json values (lowest precedence)
# =========================================================================

# Core application settings (non-sensitive)
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:80

# Database Configuration - Default PostgreSQL (non-sensitive)
POSTGRES_DB=emma
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
CONNECTION_STRINGS__POSTGRESQL=Host=${POSTGRES_HOST};Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}

# -------------------------------------------------------------------
# SENSITIVE CONFIGURATIONS - Use placeholders in this file
# Real values should go to .env.local (which is in .gitignore)
# -------------------------------------------------------------------

# CosmosDB Configuration (SENSITIVE)
COSMOSDB__ACCOUNTENDPOINT=placeholder-endpoint
COSMOSDB__ACCOUNTKEY=placeholder-key
COSMOSDB__DATABASENAME=placeholder-database
COSMOSDB__CONTAINERNAME=placeholder-container

# Azure AI Foundry Configuration (SENSITIVE)
AZUREAIFOUNDRY__ENDPOINT=placeholder-endpoint
AZUREAIFOUNDRY__APIKEY=placeholder-key
AZUREAIFOUNDRY__DEPLOYMENTNAME=placeholder-deployment
AZUREAIFOUNDRY__MAXTOKENS=1000
AZUREAIFOUNDRY__TEMPERATURE=0.7

# Azure OpenAI Configuration (SENSITIVE)
AZUREOPENAI__APIKEY=placeholder-key
AZUREOPENAI__ENDPOINT=placeholder-endpoint
AZUREOPENAI__DEPLOYMENTNAME=placeholder-deployment
AZUREOPENAI__APIVERSION=2023-05-15

# Azure Storage Configuration (SENSITIVE)
AZURESTORAGE__ACCOUNTNAME=placeholder-name
AZURESTORAGE__ACCOUNTKEY=placeholder-key

# Azure Search Configuration (SENSITIVE)
AZURESEARCH__SERVICENAME=placeholder-name
AZURESEARCH__APIKEY=placeholder-key
AZURESEARCH__ENDPOINT=placeholder-endpoint

# Key Vault Configuration (SENSITIVE)
KEYVAULT__NAME=placeholder-name
